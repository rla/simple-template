:- load_files([
    tests/file,
    tests/expr,
    tests/funs,
    tests/tokens,
    tests/semblance_tokens,
    tests/user_defined_tokens,
    tests/blocks,
    tests/render
], [ if(not_loaded) ]).
